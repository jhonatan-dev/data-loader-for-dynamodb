{
  "name": "data-loader-for-dynamodb",
  "version": "1.0.0",
  "description": "Load data to your table of DynamoDB.",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "start": "node index.js",
    "gui-local": "DYNAMO_ENDPOINT=http://localhost:4566 AWS_REGION=eu-west-1 AWS_ACCESS_KEY_ID=local AWS_SECRET_ACCESS_KEY=local dynamodb-admin",
    "gui-local-win": "set DYNAMO_ENDPOINT=http://localhost:4566 && set AWS_REGION=eu-west-1 && set AWS_ACCESS_KEY_ID=local && set AWS_SECRET_ACCESS_KEY=local && dynamodb-admin",
    "test": "echo \"Tests not for now!\""
  },
  "keywords": [
    "data-loader-for-dynamodb"
  ],
  "author": "jhonatan-dev",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1090.0",
    "express": "^4.17.1",
    "swagger-jsdoc": "^6.1.0",
    "swagger-ui-express": "^4.3.0"
  },
  "devDependencies": {
    "dynamodb-admin": "^4.1.0",
    "nodemon": "^2.0.12"
  }
}